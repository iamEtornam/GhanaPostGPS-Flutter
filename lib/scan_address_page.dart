import 'package:flutter/material.dart';

class ScanAddressPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: ListView(
        children: <Widget>[
          Center(child: Text('Scan qrcode or ghanapost gps code '))
        ],
      ),
    );
  }
}
